<h2>Sửa thông tin
    <%= resource_name.to_s.humanize %></h2>

<%= form_for(resource, as: resource_name, url: registration_path(resource_name),
html: { method: :put, class: 'form-horizontal' }) do |f| %>
<%= devise_error_messages! %>

    <div class="form-group">
        <%= f.label :"Ảnh đại diện", class: 'col-sm-2 control-label' %>
        <br/>
        <%= image_tag @user.avatar.url , size: "120x160"%>
    </div>

    <div class="form-group">
        <%= f.label :"Sửa ảnh", class: 'col-sm-2 control-label' %>
        <br/>
        <%= f.file_field :avatar %>
    </div>

    <div class="form-group">
        <%= f.label :email, class: 'col-sm-2 control-label' %>
        <div class="col-sm-6">
            <%= f.email_field :email, class: 'form-control' , :disabled => "true" %>
        </div>
    </div>

    <div class="form-group">
        <%= f.label :name, class: 'col-sm-2 control-label' %>
        <div class="col-sm-6">
            <%= f.text_field :name, autofocus: true, class: 'form-control' %>
        </div>
    </div>

    <div class="form-group">
        <%= f.label :password, class: 'col-sm-2 control-label' %>
        <i>(ít nhất 6 kí tự)</i>
        <div class="col-sm-6">
            <%= f.password_field :password, autocomplete: "off", class: 'form-control' %>
        </div>
    </div>

    <div class="form-group">
        <%= f.label :password_confirmation, class: 'col-sm-2 control-label' %>
        <div class="col-sm-6">
            <%= f.password_field :password_confirmation, autocomplete: "off", class:
            'form-control' %>
        </div>
    </div>

    <div class="form-group">
        <%= f.label :current_password, class: 'col-sm-2 control-label' %>
        <i>(mật khẩu cũ)</i>
        <div class="col-sm-6">
            <%= f.password_field :current_password, autocomplete: "off", class:
            'form-control' %>
        </div>
    </div>

    <div class="form-group">
        <div class="col-sm-offset-2 col-sm-6">
            <button type="submit" class="btn btn-default">Lưu tài khoản</button>
        </div>
    </div>


<% end %>

<% if current_user%>
<% if current_user.role == "admin"%>
   <!-- <h3>Xóa tài khoản</h3>
    <%= link_to "","/"%> -->
    <%#= button_to "Xóa tài  khoản", registration_path(resource_name), data: { confirm: "Bạn chắt chắn xóa ?" }, method: :delete, class: "btn btn-default" %><% end %>
<% end %>